{
  "name": "abdul-khoir-landing-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "rm -rf assets IconDashboard subnet-calculate",
    "dev": "vite",
    "build": "vite build && cp dist/index.html . && cp -R dist/assets . && cp -R dist/IconDashboard . && cp -R dist/subnet-calculate .",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.0"
  }
}
